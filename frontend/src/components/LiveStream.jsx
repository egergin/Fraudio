import React from 'react';
const money = (value) => `₺${Number(value || 0).toLocaleString('tr-TR', { minimumFractionDigits: 2, maximumFractionDigits: 2 })}`;
const time = (value) => value ? new Date(value).toLocaleTimeString('tr-TR', { hour: '2-digit', minute: '2-digit', second: '2-digit' }) : 'az önce';
export default function LiveStream({ transactions, onSelectUser }) { return <section className="panel"><div className="panel-heading"><div><p className="eyebrow">CANLI TELEMETRİ</p><h3>İşlem akışı</h3></div><span className="live-label"><i /> LIVE</span></div>{transactions.length === 0 ? <div className="state-block"><strong>Akış bekleniyor</strong><span>WebSocket üzerinden yeni işlemler geldiğinde burada görünecek.</span></div> : <div className="stream-list">{transactions.map((tx, index) => <article className={`stream-item ${tx.status?.toLowerCase() === 'suspicious' ? 'item-alert' : ''}`} key={tx.transactionId || index}><div className="stream-main"><button className="stream-user-link" onClick={() => onSelectUser(tx.userId)}>{tx.userId || 'Bilinmiyor'}</button><span className={`badge-status badge-${tx.status?.toLowerCase() === 'suspicious' ? 'suspicious' : tx.status?.toLowerCase() === 'approved' ? 'approved' : 'received'}`}>{tx.status === 'Suspicious' ? 'ŞÜPHELİ' : tx.status === 'Approved' ? 'ONAYLI' : 'ALINDI'}</span><small>{tx.city || 'Konum bilinmiyor'}</small></div><div className="stream-meta"><strong>{money(tx.amount)}</strong><small>{time(tx.occurredAt)}</small></div></article>)}</div>}</section>; }
